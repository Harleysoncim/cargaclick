#!/usr/bin/env bash
set -euo pipefail
: "${DATABASE_URL:?DATABASE_URL is required}"
if [ "${FLY_MIGRATIONS_ENABLED:-false}" != true ]; then
  echo 'Migration blocked: approve and verify the destination database before enabling FLY_MIGRATIONS_ENABLED.' >&2
  exit 1
fi
exec bundle exec rails db:migrate
