#!/usr/bin/env bash
set -euo pipefail
: "${DATABASE_URL:?DATABASE_URL is required}"
if [ "${FLY_MIGRATIONS_ENABLED:-false}" != true ]; then
  echo 'Migrations skipped: FLY_MIGRATIONS_ENABLED is not true. Deploying code without running db:migrate.' >&2
  exit 0
fi
exec bundle exec rails db:migrate
